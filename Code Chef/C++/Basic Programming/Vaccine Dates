#include <iostream>
using namespace std;

int main() {
	int t,x,y,z;
	cin>>t;
	for(int i=0;i<t;i++)
	{
	    cin>>x>>y>>z;
	    if(x>=y && x<=z)cout<<"Take second dose now"<<endl;
	    else if (x>z)cout<<"Too Late"<<endl;
	    else if (x<y)cout<<"Too Early"<<endl;
	}
	return 0;
}
