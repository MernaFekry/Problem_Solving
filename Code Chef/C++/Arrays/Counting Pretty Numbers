#include <iostream>
using namespace std;

int main() {
	int t,x,y,count=0;
	cin>>t;
	for(int i=0;i<t;i++)
	{
	    cin>>x>>y;
	    for(int j=x;j<=y;j++)
	    {
	        if(j%10==2 ||j%10==3 || j%10==9)count++;
	        
	    }
	    cout<<count<<endl;
	    count=0;
	}
	return 0;
}
